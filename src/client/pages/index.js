import Head from 'next/head'

export default function Home() {
  return (
    <div>
      <Head>
        <title>Shiggy's Office</title>
        <link rel="icon" href="/favicon.ico" />
      </Head>
      
    </div>
  )
}

if (typeof window !== "undefined") {

  localStorage.setItem("lmao", "sex")
  
  }
